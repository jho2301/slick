node_modules/
dist/
out/
shots/
*.log

# TypeScript build mode writes declarations here and nothing runs from them.
.tsbuild/
*.tsbuildinfo

# The built web UI. The daemon serves it from here; `npm run build` makes it.
packages/server/public/
apps/web/dev-dist/
.vite/
coverage/
.eslintcache

# A workspace accidentally created inside the repo
.slick/
*.db
*.db-wal
*.db-shm

.DS_Store

# Hermes loads plugins/hermes/slick through a symlink from ~/.hermes/plugins,
# so the gateway compiles into the repo. The repo is the single source of truth
# for the plugin; its bytecode is not.
__pycache__/
*.pyc
